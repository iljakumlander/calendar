import { FormatDateOptions } from "@fullcalendar/core";

export const formatDateConfig: FormatDateOptions = {
    month: 'long',
    year: 'numeric',
    day: 'numeric',
    weekday: 'long',
    hour: 'numeric',
    minute: '2-digit',
    hour12: false,
};
